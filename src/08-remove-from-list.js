/* function removeKFromList(l, k) {
  console.log(l);
  console.log(k);
  const arr = l.splice();

  for (let i = 0; i < arr.length; i++) {
    if (arr[i] === k) arr.splice(i, 1);
  }
  return arr;
}

module.exports = removeKFromList;
 */
